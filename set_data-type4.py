print("The data type's unordered collection is Python Set. It is iterable, mutable(can change after creation), and has remarkable components.")

print("----------------------------------------------------------------------------------------------------")

print("The elements of a set have no set order; It might return the element's altered sequence.")

print("----------------------------------------------------------------------------------------------------")

print("Either a sequence of elements is passed through the curly braces and separated by a comma to create the set or the built-in function set() is used to create the set.")

print("----------------------------------------------------------------------------------------------------")

print(" It can contain different kinds of values.")


set1 = set()  
print(set1)